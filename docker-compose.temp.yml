version: '3.7'

services:
  django_temp:
    build: ./app
    volumes:
      - ./app/:/code
    ports:
      - 8000:8000
    command: "python manage.py runserver 0.0.0.0:8000"
    env_file: environment_variable/settings.env
